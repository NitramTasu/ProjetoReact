import React from 'react';
import './style.css'

const Button = ({ label }) => (
    <button class="mdl-button mdl-js-button mdl-button--raised mdl-button--colored">{label}</button>
);

export default Button